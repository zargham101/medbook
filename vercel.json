{
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/index.ts?path=$1" }
  ],
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ],
  "crons": [
    { "path": "/api/cron/remind", "schedule": "*/15 * * * *" }
  ]
}
